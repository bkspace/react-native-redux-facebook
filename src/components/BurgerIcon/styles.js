import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  burgerButton: {
    width: 100,
    height: 37,
    position: 'absolute',
    bottom: 4,
    left: 2,
    padding: 8,
  },
  burgerIcon: {
    fontSize: 22,
    color: '#fff',
  },
});
